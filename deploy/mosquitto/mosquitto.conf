# Development broker only. Production uses the existing broker on the network.
# Anonymous access is acceptable here because this container is not deployed;
# the real broker uses username and password (ADR-002).
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
